#root is country
#scope:target is the target 

cb_humiliate_rival = {
	visible = {
		scope:target = {
			is_rival_of = root
		}
	}
	allow_creation = {
		not = {
			has_truce_with = scope:target
		}
	}

	war_goal_type = humiliate
	ai_selection_desire = -10 # can cause a lot of useless wars for ai
}
