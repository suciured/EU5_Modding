namespace = flavor_psk
#The independence of Pskov
flavor_psk.1 = {
	type = country_event
	title = flavor_psk.1.title
	desc = flavor_psk.1.desc

	illustration_tags = {
        10 = regular
        10 = interior
    }

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
	}
	
	#We shall become a Republic!
	option = {
		name = flavor_psk.1.a
		change_government_type = government_type:republic
		research_advance = advance_type:veche_advance
		change_heir_selection = heir_selection:veche_selection
		add_reform = government_reform:veche_republic
		ai_chance = {
			factor = 9
		}
	}
	
	#Our current government is fine enough
	option = {
		name = flavor_psk.1.b
		add_government_power = government_power_extreme_bonus
		ai_chance = {
			factor = 1
		}
	}
}