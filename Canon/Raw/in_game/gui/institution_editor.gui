window = {
	name = "institution_editor"
	parentanchor = top|left
	size = { 400 70% }
	position = { 10 200 }
	widgetanchor = top|left
	movable = yes
	resizable = yes
	using = editor_window

	vbox = {
		spacing = 5
		margin = { 10 5 }
		text_single = {
			layoutpolicy_horizontal = expanding
			raw_text = "Institution Setup"
		}

		text_single = {
			layoutpolicy_horizontal = expanding
			text = "[InstitutionEditor.GetCurrent]"
		}

		scrollarea = {
			using = layoutpolicy_expanding
			scrollbarpolicy_horizontal = always_off

			scrollbar_vertical = {
				using = editor_vertical_scrollbar
			}

			scrollwidget = {
				fixedgridbox = {
					using = layoutpolicy_expanding
					name = "buildings_grid"

					addcolumn = 100%
					addrow = 31
					setitemsizefromcell = yes

					
					datamodel = "[InstitutionEditor.GetEntries]"
					item = {
						editor_button = {
							text = "[InstitutionEntry.GetName]"
							onclick = "[InstitutionEntry.OnClick]"
							using = editor_text
						}
					}
				}
			}
		}
	}

	editor_button = {
		parentanchor = right|top
		position = { -5 5 }
		raw_text = "X"
		onclick = "[ExecuteConsoleCommand('institution_editor')]"
	}
}

