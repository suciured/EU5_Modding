select_menu_left = {
	blockoverride "menu_setup" {
		name = "select_heir_selection"
	}

	blockoverride "close_button" {
		onclick = "[SelectHeirSelection.OnClose]"
	}

	blockoverride "select_content" {
		expand = {}
	}
}
