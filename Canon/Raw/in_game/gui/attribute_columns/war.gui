types WarColumns
{
	type war_name = select_cell_default {
		size = { 150 32 }

		datacontext = "[InteractionTarget.GetWar]"

		sort_by_highlight = {
			size = { 100% 100% }
			name = "name"
		}

		text_single = {
			size = { 100% 100% }
			margin = { 10 0 }
			autoresize = no
			align = nobaseline
			default_format = "#yellow_titles"
			text = "[InteractionTarget.GetWar.GetNameWithNoTooltip]"
		}
	}
}
